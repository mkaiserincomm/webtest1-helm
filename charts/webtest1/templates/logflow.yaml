apiVersion: logging.banzaicloud.io/v1beta1
kind: Flow
metadata:  
  name: {{ include "webtest1.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  globalOutputRefs: []
  localOutputRefs:
  - {{ include "webtest1.fullname" . }}
  match:
  - select:
      container_names: []
      hosts: []
      labels:
        app.kubernetes.io/name: {{ include "webtest1.fullname" . }}